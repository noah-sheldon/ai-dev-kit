# Contributing

## Workflow

1. Make a focused change.
2. Run `npm test`.
3. Update docs if the surface changes.
4. Keep manifests, schemas, and examples in sync.

## Standards

- ASCII-only files unless a file already uses non-ASCII content.
- Keep plugin manifests valid JSON.
- Treat `scripts/validate-surface.js` failures as blockers.
