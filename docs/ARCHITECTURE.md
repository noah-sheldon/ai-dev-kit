# Architecture

The repo is organized around installable surfaces:

- `agents/`, `skills/`, `commands/`, and `hooks/`
- `rules/`, `manifests/`, `schemas/`, and `mcp-configs/`
- plugin manifests for Codex, Claude, and OpenCode
