---
name: build-fix
description: Fix build and type failures with minimal diffs.
---
# Build Fix

Fix build and type failures with minimal diffs.

## Delegate

Apply the `build-error-resolver` skill.

## Notes

- Fix only the smallest set of build or type errors needed to restore green.
- Keep responses short and direct.
- Prefer the maintained skill surface over duplicated prompt logic.
