---
name: install
description: Install the AI Dev Kit surface into a target directory.
argument-hint: [target]
---
# Install

Install the AI Dev Kit surface into a target directory.

## Delegate

Apply the `install-apply` skill.

## Notes

- Keep the workflow bounded and explicit.
- Keep responses short and direct.
- Prefer the maintained skill surface over duplicated prompt logic.
